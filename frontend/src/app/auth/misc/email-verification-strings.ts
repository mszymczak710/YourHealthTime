export const emailVerificationStrings = Object.freeze({
  emailVerification: {
    succeed: 'Adres e-mail został pomyślnie zweryfikowany',
    title: 'Weryfikacja adresu e-mail'
  }
});

export type EmailVerificationStrings = typeof emailVerificationStrings;
