export const diseaseStrings = Object.freeze({
  disease: {
    name: {
      label: 'Nazwa',
      placeholder: 'Wpisz nazwę'
    },
    readable_id: {
      label: 'ID',
      placeholder: 'Wpisz ID'
    }
  }
});

export type DiseaseStrings = typeof diseaseStrings;
