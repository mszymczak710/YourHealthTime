export const specializationStrings = Object.freeze({
  specialization: {
    name: {
      label: 'Nazwa',
      placeholder: 'Wpisz nazwę'
    },
    readable_id: {
      label: 'ID',
      placeholder: 'Wpisz ID'
    }
  }
});

export type SpecializationStrings = typeof specializationStrings;
