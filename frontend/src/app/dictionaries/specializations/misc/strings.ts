import { specializationStrings } from './specialization-strings';

export const strings = Object.freeze({
  listHeader: 'Specjalizacje',
  specialization: specializationStrings.specialization
});

export type SpecializationListStrings = typeof strings;
